import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    disabledContainer: {
        padding: 8,
        opacity: 0.3,
        marginLeft: 8,
        marginTop: 9,
        flex: 1
    }
});
