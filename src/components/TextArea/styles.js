import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 8,
        flex: 1,
        flexDirection: 'column',
        paddingBottom: 0,
        paddingTop: 0
    },
    input: {
        marginLeft: 10,
        marginRight: 10,
        borderRadius: 5,
        borderWidth: 0.5,
        borderColor: '#bdbec1'
    }
});
