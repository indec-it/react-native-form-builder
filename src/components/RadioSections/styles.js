import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 4
    },
    sectionTitle: {
        fontWeight: 'bold',
        padding: 10
    }
});
